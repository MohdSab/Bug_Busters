# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build chat-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t chat-service`.
FROM docker.io/node:lts-alpine

ENV NODE_ENV development

WORKDIR /app

COPY package.json .
RUN npm install
RUN npm install -g nx@latest
RUN npm install socket.io
RUN npm i @nestjs/platform-socket.io

COPY apps/chat-service/ apps/chat-service/

COPY nx.json .
COPY tsconfig.base.json .
COPY libs/ libs/

EXPOSE 4200

CMD [ "nx", "serve", "chat-service" ]
