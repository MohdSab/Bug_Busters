# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3010

WORKDIR /app

RUN addgroup --system auth-service && \
  adduser --system -G auth-service auth-service

COPY package.json .
# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --omit=dev install

COPY dist/apps/auth-service auth-service

CMD [ "node", "auth-service/main.js" ]
