# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build gamehub`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t gamehub`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3020

WORKDIR /app

RUN addgroup --system gamehub && \
          adduser --system -G gamehub gamehub


COPY package.json .
# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm gamehub --omit=dev -f install

COPY dist/apps/gamehub gamehub
RUN chown -R gamehub:gamehub .

CMD [ "node", "gamehub/main.js" ]
