# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build ttt-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t ttt-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

COPY package.json .
COPY package-lock.json .
RUN npm --omit=dev install

COPY dist/apps/ttt-service ttt-service
# RUN chown -R ttt-service:ttt-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

CMD [ "node", "ttt-service/main.js" ]
