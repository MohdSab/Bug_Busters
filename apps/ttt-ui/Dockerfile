# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build ttt-ui`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
FROM docker.io/node:lts-alpine

ENV NODE_ENV development

WORKDIR /app

RUN addgroup --system ttt-ui && \
  adduser --system -G ttt-ui ttt-ui

COPY package.json .
COPY package-lock.json .
RUN npm install
RUN npm install -g nx@latest

COPY apps/ttt-ui .
COPY nx.json .

EXPOSE 4200

CMD [ "nx", "serve ttt-ui" ]

# COPY dist/apps/ttt-ui ttt-ui
# RUN chown -R ttt-ui:ttt-ui .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
# RUN npm --prefix ttt-ui --omit=dev -f install

# CMD [ "node", "ttt-ui" ]
